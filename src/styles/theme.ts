import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
	// fonts: {
	//   heading: "Syne",
	//   body: "Syne",
	// },
	colors: {
		primary: "#FF00B5",
	},
});

export default theme;
